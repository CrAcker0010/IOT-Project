# ═══════════════════════════════════════════════════════════════════
#  IOT Robot — Python Dependencies
#  Target: Raspberry Pi 4 (1 GB RAM, 32-bit OS / armv7l)
#
#  Optimized for low-memory environment.
# ═══════════════════════════════════════════════════════════════════

# ── Core Hardware ──────────────────────────────────
RPi.GPIO==0.7.1
smbus2==0.4.2

# ── Camera & Vision ────────────────────────────────
# Headless version avoids X11 deps and saves ~200MB RAM
opencv-python-headless==4.10.0.84
Pillow>=9.0.0,<11.0.0
numpy<2.0.0

# ── AI & Intelligence ──────────────────────────────
google-genai>=1.0.0
# YOLOv8 (Heavy for 1GB RAM, use nano models)
ultralytics>=8.1.0

# ── Web Dashboard ──────────────────────────────────
Flask==2.3.3
requests==2.31.0

# ── Audio & Voice ──────────────────────────────────
SpeechRecognition==3.10.0
# PyAudio requires 'sudo apt-get install libasound-dev portaudio19-dev'
PyAudio==0.2.14

# ── Peripheral Drivers ─────────────────────────────
RPLCD>=1.3.0
pynmea2==1.19.0
pyserial==3.5
